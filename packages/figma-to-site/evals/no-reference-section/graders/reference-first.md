---
type: llm
criteria: 'Does the answer press for the reference to be captured first (or at minimum say plainly that building from copy alone will produce guessed layout), and — if it does build the component anyway — record the section as unverified in the trust manifest and state the gap explicitly in its reply, rather than delivering the component as though it were checked? Does it avoid silently inferring the layout from the copy and leaving no trace?'
focus: 'Whether the gap is made visible. Building the component is acceptable; building it and handing it over as done is not. The deciding evidence is whether the reply states, unprompted, that this section is not verified against the design.'
---

This exact shortcut shipped a CTA section with wrong vertical rhythm and a button
hugging its label instead of stretching to the text column. It was built from copy
alone and the only record was a comment in the file nobody read.

Note that "I can eyeball it later" is the user's own plan, and the answer does not
have to refuse it. It has to leave a trace that survives the person who wrote it.
