{
  "name": "deploy-to-vercel",
  "version": "0.1.0",
  "private": true,
  "description": "A skill that walks a user through deploying a Next.js app to Vercel from a fresh machine, and through verifying the deployment rather than stopping at a green build.",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "files": [
    "SKILL.md",
    "README.md",
    "evals"
  ],
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
